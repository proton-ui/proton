import pDropdown from './pDropdown'
import pDropdownItem from './pDropdownItem'

export default Vue => {
    Vue.component(pDropdown.name, pDropdown)
    Vue.component(pDropdownItem.name, pDropdownItem)
}