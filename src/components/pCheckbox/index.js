import pCheckbox from './pCheckbox'
import pCheckboxGroup from './pCheckboxGroup'

export default Vue => {
    Vue.component(pCheckbox.name, pCheckbox)
    Vue.component(pCheckboxGroup.name, pCheckboxGroup)
}